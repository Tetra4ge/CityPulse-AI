# CityPulse AI — GPU Service Dependencies
#
# RAPIDS cuDF & cuML require Linux/WSL2 with NVIDIA WDDM driver.
# If running locally on Windows, the service transparently falls back to
# pandas and scikit-learn (CPU).
#
# GPU-Only RAPIDS libraries (uncomment/install inside Linux/WSL GPU runtime):
# cudf-cu12>=24.4.0
# cuml-cu12>=24.4.0

fastapi>=0.100.0
uvicorn>=0.20.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.2.0
requests>=2.30.0
modal>=0.62.0
